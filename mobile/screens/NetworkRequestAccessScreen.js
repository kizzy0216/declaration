import React from 'react';
import { View, Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

function NetworkRequestAccessScreen({ navigation }) {
  return (
    <SafeAreaView>
      <Text>NetworkRequestAccessScreen</Text>
    </SafeAreaView>
  );
}

export default NetworkRequestAccessScreen;
